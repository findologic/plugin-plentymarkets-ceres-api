{{ component( "Ceres::ItemList.Components.Filter.ItemFilterList") }}

<script src="{{ plugin_path("Findologic") }}/js/dist/filters-component.js"></script>

<div class="findologic-container">
    {% if resultsCount > 0 %}
        <item-filter-list :facet-data="{{ facets | json_encode }}"></item-filter-list>
    {% else %}
        {% include "Findologic::content.NoResults" %}
    {% endif %}
</div>