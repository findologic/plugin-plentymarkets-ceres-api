<div class="row mt-3">
    <smart-did-you-mean
            :smart-text="{{ smart_did_you_mean | json_encode}}"
            :info-data="{{  query_info_message | striptags('<strong>,<a>') | raw | json_encode }}"
    />
</div>
