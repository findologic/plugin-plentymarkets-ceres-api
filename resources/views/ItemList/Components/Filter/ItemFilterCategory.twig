<script type="x/template" id="vue-item-filter-category">
    <div class="form-check" v-for="value in facets" :key="value.id">
        <div class="category-container">
            <input :id="'option-' + value.id" class="form-check-input hidden-xs-up" type="checkbox" :checked="isSelected(value)" @change="updateFacet(value)" :disabled="isLoading">
            <label :for="'option-' + value.id" class="form-check-label">
                ${ value.name }
            </label>
            <div class="filter-badge">${ value.count }</div>
        </div>
        <div class="sub-category-container" v-if="value.items.length">
            <div class="form-check" v-for="subCategory in value.items" :key="subCategory.id">
                <input :id="'option-' + subCategory.id" class="form-check-input hidden-xs-up" type="checkbox" :checked="isSelected(getSubCategoryValue(value, subCategory))" @change="updateFacet(getSubCategoryValue(value, subCategory))" :disabled="isLoading">
                <label :for="'option-' + subCategory.id" class="form-check-label">
                    ${ subscategory.name }
                </label>
            </div>
        </div>
    </div>
</script>