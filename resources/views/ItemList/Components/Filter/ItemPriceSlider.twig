<script type="x/template" id="vue-item-price-slider">
    <div class="container fl-price-range-slider-container">
        <div class="row">
            <div class="col-md-6 col-xs-6">
                <input class="price-range-input" v-model="priceFrom">
                <span class="currency-sign">${facet.currency}</span>
            </div>
            <div class="col-md-6 col-xs-6">
                <input class="price-range-input" v-model="priceTo">
                <span class="currency-sign">${facet.currency}</span>
            </div>
        </div>
        <div class="row price-range-slider-row">
            <div class="col-md-9 col-sm-9 col-xs-12">
                <div id="price-slider-range"></div>
            </div>
            <div class="col-md-3 col-sm-3 col-xs-12">
                <button type="button" class="btn btn-primary range-slider-submit-btn" :class="{'disabled': isDisabled}" v-tooltip
                        data-toggle="tooltip" data-placement="top" @click="triggerFilter()" title="{{ trans("Ceres::Template.itemApply") }}">
                    <i v-waiting-animation="isLoading" class="fa fa-check" aria-hidden="true"></i>
                </button>
            </div>
        </div>
    </div>
</script>